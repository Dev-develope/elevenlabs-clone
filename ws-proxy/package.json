{
  "name": "elevenlabs-clone-ws-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Sidecar that proxies browser WS connections to wss://api.60db.ai/ws/tts, hiding SIXTYDB_API_KEY behind a short-lived signed token issued by the Next.js app.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
